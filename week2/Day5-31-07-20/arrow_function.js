const joinFirstLastName = ({ firstName, lastName }) =>console.log( person.firstName + '-' + person.lastName);

const person = {
  firstName: "GUVI",
  lastName: "GEEK",
  age: 3,
  sex: "M"
}
joinFirstLastName(person);
